\chapter{Arhitectura Aplicației}
\label{chap:ch4}

\section{Prezentare Generală}

Aplicația este structurată ca un sistem modular de procesare video în timp real, cu scopul de a detecta planul solului și de a planifica o traiectorie navigabilă pe baza unei hărți de adâncime generate de un model de învățare profundă. Sistemul suportă două profiluri hardware distincte: un host cu GPU NVIDIA, folosind PyTorch și CUDA pentru inferență, respectiv un Raspberry Pi 5 cu accelerator NPU Hailo-8, folosind biblioteca \texttt{hailort}. Indiferent de profilul activ, restul pipeline-ului rămâne identic, ceea ce face posibilă portabilitatea codului fără modificări.

Întreaga bază de cod este scrisă în Python 3.11+ și organizată în două mari componente: un \textbf{backend} de procesare și o \textbf{interfață grafică} (GUI) construită cu PyQt6. Separarea clară dintre aceste două componente permite înlocuirea sau testarea fiecăreia independent.

\section{Tehnologii Utilizate}

Implementarea sistemului se bazează pe un set de biblioteci și framework-uri specializate, selectate în funcție de cerințele de performanță și de compatibilitatea cu cele două platforme hardware vizate. Tabelul~\ref{tab:tech} prezintă o sinteză a acestora, împreună cu versiunea utilizată și rolul fiecăreia în arhitectura aplicației.

\begin{table}[htbp]
\centering
\renewcommand{\arraystretch}{1.35}
\begin{tabular}{|l|l|p{7cm}|}
\hline
\textbf{Tehnologie} & \textbf{Versiune} & \textbf{Rol în aplicație} \\
\hline
Python & 3.11+ & Limbajul principal; utilizat pentru întreaga bază de cod, beneficiind de \texttt{dataclasses}, type hints și \texttt{tomllib} nativ \\
\hline
PyQt6 & 6.x & Interfața grafică multi-platformă; mecanismul de semnale și sloturi asigură comunicarea thread-safe între fire de execuție \\
\hline
OpenCV & 4.x & Captură video (V4L2), corecție de distorsiune, calibrare cu tablă de șah \\
\hline
NumPy & 2.x & Procesare numerică vectorizată a hărților de adâncime, măștilor și coordonatelor 3D \\
\hline
PyTorch + CUDA & 2.x & Backend de inferență pe GPU NVIDIA; suportă AMP și \texttt{torch.compile} \\
\hline
Hailo Runtime & 4.x & Backend de inferență pe NPU Hailo-8 integrat în Raspberry Pi AI HAT+ \\
\hline
TOML & — & Format de configurație structurată, citit nativ via \texttt{tomllib} (Python 3.11+) \\
\hline
\end{tabular}
\caption{Tehnologiile principale utilizate în implementare}
\label{tab:tech}
\end{table}

\section{Principii de Proiectare Orientată pe Obiecte}

\subsection{Separarea Responsabilităților}

Fiecare modul al aplicației are o singură responsabilitate bine definită. Modulul \texttt{config.py} se ocupă exclusiv de citirea și validarea configurației. Modulul \texttt{hardware.py} abstractizează diferențele dintre platformele hardware. Modulul \texttt{ground.py} conține exclusiv logica de detectare a planului solului, fără nicio referință la interfața grafică sau la camera video. Această separare face ca fiecare componentă să poată fi testată, înlocuită sau extinsă fără a afecta restul sistemului.

\subsection{Încapsulare și Gestiunea Stării}

Clasele sunt folosite acolo unde există stare internă care trebuie gestionată pe durata vieții unui obiect. \texttt{HardwareProfile} encapsulează profilul hardware activ și expune proprietăți derivate (\texttt{is\_nvidia}, \texttt{is\_rpi}, \texttt{is\_metric}) fără a expune câmpurile interne. \texttt{CalibrationService} grupează operațiile de calibrare împreună cu configurația de care au nevoie. \texttt{DepthPipeline} gestionează întregul ciclu de viață al procesării unui cadru video: deschiderea camerei, încărcarea modelului, firul de captură, și eliberarea resurselor la oprire.

Funcțiile pure sunt preferate claselor atunci când nu există stare internă de gestionat. Modulele \texttt{ground.py}, \texttt{path.py} și \texttt{visualization.py} sunt compuse exclusiv din funcții, deoarece transformările pe care le realizează (proiecție 3D, RANSAC, A*, suprapunere vizuală) sunt fără efecte secundare și nu necesită obiecte.

\subsection{Configurație Tipizată prin Dataclasses}

Configurația aplicației este citită o singură dată la pornire din fișierul \texttt{config.toml} și transformată într-un arbore de \texttt{dataclass}-uri imutabile (\texttt{frozen=True}): \texttt{App\allowbreak Config}, \texttt{Camera\allowbreak Config}, \texttt{Calibration\allowbreak Config}, \texttt{Ground\allowbreak Config}, \texttt{Profiling\allowbreak Config} etc. Această abordare elimină accesul prin chei de tip șir de caractere și înlocuiește șirul de caractere cu atribute tipizate, ceea ce permite detectarea erorilor de configurare la încărcare, nu la rulare, și oferă autocompletare în mediile de dezvoltare.

\subsection{Comunicare Asincronă prin Semnale Qt}

Inferența modelului de adâncime și captura video rulează în fire de execuție separate față de firul principal al interfeței grafice. Comunicarea dintre fire se realizează exclusiv prin mecanismul de semnale și sloturi al Qt (\texttt{pyqtSignal}), care garantează că actualizările interfeței grafice au loc întotdeauna pe firul principal. Această abordare previne blocarea interfeței în timpul procesării și elimina condițiile de cursă (\textit{race conditions}) specifice accesului concurent la widget-uri.

\subsection{Inversiunea Dependențelor la Nivel de Backend}

Modulul \texttt{model.py} acționează ca un registru de factory: în funcție de profilul hardware activ, deleghează apelurile fie către \texttt{model\_nvidia.py}, fie către \texttt{model\_rpi.py}. Pipeline-ul central (\texttt{pipeline.py}) depinde exclusiv de interfața abstractă a lui \texttt{ModelRegistry}, nu de implementările concrete. Adăugarea unui nou backend necesită doar adăugarea unui fișier nou și a unui caz în registru, fără a modifica pipeline-ul sau GUI-ul.

\section{Diagrama de Clase}

Figura~\ref{fig:classdiagram} prezintă clasele principale ale aplicației și relațiile dintre acestea. Conform notației UML, săgețile continue reprezintă \emph{asocieri}: clasa sursă păstrează o referință de durată (un câmp) către clasa țintă. Săgețile întrerupte reprezintă \emph{dependențe}: clasa sursă doar creează sau folosește tranzitoriu un obiect, fără a-l reține ca stare. Relația de instanțiere este marcată suplimentar cu stereotipul standard $\ll$\textit{create}$\gg$.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    >=Stealth,
    assoc/.style={->, thick},
    dep/.style={->, dashed, thick},
    every node/.style={font=\scriptsize}
]

\tikzset{
    umlclass/.style={
        rectangle split, rectangle split parts=3,
        draw, fill=blue!6,
        rectangle split part align={center, left, left},
        inner sep=3pt, minimum width=4cm, text width=3.8cm
    },
    umldto/.style={
        rectangle split, rectangle split parts=2,
        draw, fill=orange!8,
        rectangle split part align={center, left},
        inner sep=3pt, minimum width=4cm, text width=3.8cm
    }
}

% Row 1
\node[umlclass] (CalibSvc) at (0, 9) {
    \textbf{CalibrationService}
    \nodepart{two}
    \texttt{-cfg: AppConfig}
    \nodepart{three}
    \texttt{+load(): (mtx, dist)}\\
    \texttt{+run\_interactive()}
};

\node[umlclass] (AppConfig) at (5.5, 9) {
    \textbf{AppConfig}
    \nodepart{two}
    \texttt{+hardware: HardwareConfig}\\
    \texttt{+camera: CameraConfig}\\
    \texttt{+ground: GroundConfig}
    \nodepart{three}
    \texttt{+load(path): AppConfig}
};

\node[umlclass] (HwProfile) at (11, 9) {
    \textbf{HardwareProfile}
    \nodepart{two}
    \texttt{+profile: str}\\
    \texttt{+is\_nvidia: bool}\\
    \texttt{+is\_rpi: bool}
    \nodepart{three}
    \texttt{+torch\_device(): str}\\
    \texttt{+from\_config(cfg)}
};

% Row 2
\node[umlclass] (SysView) at (0, 4.5) {
    \textbf{SystemViewWidget}
    \nodepart{two}
    \texttt{-pipeline: DepthPipeline}\\
    \texttt{-cfg: AppConfig}
    \nodepart{three}
    \texttt{-\_start()}\\
    \texttt{-\_stop()}
};

\node[umlclass] (Pipeline) at (5.5, 4.5) {
    \textbf{DepthPipeline}
    \nodepart{two}
    \texttt{-cfg: AppConfig}\\
    \texttt{-hw: HardwareProfile}\\
    \texttt{-model: ModelRegistry}
    \nodepart{three}
    \texttt{+start\_capture()}\\
    \texttt{+process\_next\_frame()}\\
    \texttt{+stop()}
};

\node[umlclass] (ModelReg) at (11, 4.5) {
    \textbf{ModelRegistry}
    \nodepart{two}
    \texttt{-hw: HardwareProfile}\\
    \texttt{-model: Any}
    \nodepart{three}
    \texttt{+estimate\_depth(}\\
    \texttt{\ \ frame, intrinsics)}
};

% Row 3
\node[umldto] (FrameResult) at (5.5, 0.5) {
    \textbf{FrameResult}
    \nodepart{two}
    \texttt{+rgb\_frame: ndarray}\\
    \texttt{+depth\_map: ndarray}\\
    \texttt{+ground\_mask: ndarray}\\
    \texttt{+path: ndarray}\\
    \texttt{+start\_point: tuple}\\
    \texttt{+end\_point: tuple}\\
    \texttt{+plane: ndarray}
};

% Relationships
\draw[assoc](CalibSvc.east)      -- node[above]{\textit{uses}} (AppConfig.west);
\draw[assoc](SysView.east)       -- node[above]{\textit{owns}} (Pipeline.west);
\draw[assoc](Pipeline.north)     -- node[right]{\textit{uses}} (AppConfig.south);
\draw[assoc](Pipeline.east)      -- node[above]{\textit{owns}} (ModelReg.west);
\draw[assoc](ModelReg.north)     -- node[right]{\textit{uses}} (HwProfile.south);
\draw[dep]  (Pipeline.south)     -- node[right]{$\ll$\textit{create}$\gg$} (FrameResult.north);

\end{tikzpicture}
\caption{Diagrama de clase UML a componentelor principale ale aplicației.}
\label{fig:classdiagram}
\end{figure}

\section{Arhitectura pe Straturi}

Aplicația este organizată în patru straturi distincte, cu dependențe strict direcționate de sus în jos. Stratul superior depinde de cel imediat inferior; niciun strat nu cunoaște existența straturilor superioare.

\subsection{Stratul de Prezentare (GUI)}

Corespunde pachetului \texttt{gui/} și este responsabil exclusiv de interacțiunea cu utilizatorul. Conține:
\begin{itemize}
    \item \texttt{app.py} — fereastra principală (\texttt{MainWindow}), care gestionează navigarea între ecrane printr-un \texttt{QStackedWidget};
    \item \texttt{main\_menu.py} — ecranul de pornire, care expune acțiunile principale ale aplicației: calibrarea camerei, biblioteca de calibrări, pornirea sistemului și editarea setărilor;
    \item \texttt{calibration.py} — ecranul de calibrare, inclusiv dialogul modal cu previzualizare live și afișarea scorului RMS;
    \item \texttt{calibration\_manager.py} — ecranul bibliotecii de calibrări, care expune vizual operațiile CRUD asupra calibrărilor salvate (listare, aplicare, import, export, editare a metadatelor și ștergere);
    \item \texttt{settings.py} — formularul de editare a configurației, cu scriere înapoi în \texttt{config.toml};
    \item \texttt{system\_view.py} — vizualizarea live a sistemului, cu bara laterală de opțiuni și redarea cadrelor procesate;
    \item \texttt{components.py} — widget-uri reutilizabile: \texttt{StyledButton}, \texttt{NavBar}, \texttt{ToggleSwitch}, \texttt{SectionHeader}, \texttt{StatusBadge};
    \item \texttt{styles.py} — paleta de culori, fonturile și foaia de stiluri globală Qt.
\end{itemize}

Stratul de prezentare nu realizează nicio procesare de imagine sau inferență. El pornește și oprește pipeline-ul prin metode publice și primește rezultate prin semnale Qt.

\subsection{Stratul de Orchestrare (Pipeline)}

Corespunde modulului \texttt{pipeline.py} și reprezintă punctul central de coordonare al aplicației. Clasa \texttt{DepthPipeline} gestionează:
\begin{itemize}
    \item un fir de captură dedicat, care citește continuu cadre de la cameră și păstrează întotdeauna cel mai recent cadru disponibil;
    \item un fir de inferență, care consumă cadrele, rulează pipeline-ul complet și emite rezultate prin semnale Qt;
    \item starea internă a sistemului: hărțile de corecție a distorsiunii, matricea intrinsecă activă, planul anterior detectat;
    \item un profiler de latență (\texttt{FrameProfiler}), care măsoară opțional durata fiecărei etape, detaliat în Secțiunea~\ref{sec:profiling}.
\end{itemize}

Rezultatul unui cadru procesat este reprezentat de dataclass-ul \texttt{FrameResult}, care agregă cadrul RGB, harta de adâncime, masca solului, traiectoria A*, punctele de start/stop și coeficienții planului.

\subsection{Stratul de Procesare (Core)}

Conține implementările algoritmice independente de hardware și de interfața grafică:
\begin{itemize}
    \item \texttt{ground.py} — detectarea planului solului prin RANSAC vectorizat cu eșantionare subpixelică, urmat de rafinare SVD și construirea măștii direct în spațiul 2D al imaginii;
    \item \texttt{path.py} — planificarea traiectoriei prin algoritmul A* cu conectivitate pe 8 direcții și euristică octilă admisibilă;
    \item \texttt{visualization.py} — suprapunerea vizuală a măștii solului, a traiectoriei și a barei de stare pe cadrul video;
    \item \texttt{camera.py} — deschiderea camerei (V4L2 sau picamera2), construirea hărților de corecție a distorsiunii și aplicarea lor;
    \item \texttt{calibration.py} — captarea pozelor de calibrare și rezolvarea matricei intrinseci prin \texttt{cv2.calibrateCamera}.
\end{itemize}

\subsection{Stratul de Infrastructură}

Conține modulele care nu realizează procesare propriu-zisă, ci furnizează configurație, abstractizare hardware și acces la modele:
\begin{itemize}
    \item \texttt{config.py} — citirea fișierului \texttt{config.toml} și construirea arborelui de \texttt{dataclass}-uri tipizate;
    \item \texttt{hardware.py} — \texttt{HardwareProfile}: abstractizarea profilului activ și selecția dispozitivului de calcul;
    \item \texttt{model.py} — \texttt{ModelRegistry}: factory care deleghează încărcarea și inferența modelului de adâncime;
    \item \texttt{model\_nvidia.py} — backend PyTorch + CUDA, folosind Depth Anything 3 cu AMP și \texttt{torch.compile} opțional;
    \item \texttt{model\_rpi.py} — backend Hailo, folosind \texttt{hailort} pentru inferența pe NPU-ul Hailo-8;
    \item \texttt{profiling.py} — \texttt{FrameProfiler}: instrumentarea pentru măsurarea latenței per etapă a pipeline-ului (Secțiunea~\ref{sec:profiling});
    \item \texttt{main.py} — punctul de intrare în aplicație; lansează GUI-ul sau modul headless.
\end{itemize}

\section{Diagrama Straturilor}

Figura~\ref{fig:layers} ilustrează sintetic cei patru straturi ai arhitecturii și direcția unică a dependențelor, de la interfața grafică până la infrastructură.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    layer/.style={
        draw, rounded corners=4pt,
        minimum width=10cm, minimum height=1.1cm,
        align=center, font=\small\bfseries
    },
    sublabel/.style={font=\footnotesize, text=gray}
]

\node[layer, fill=blue!12]   (gui)      at (0, 0)    {Strat de Prezentare (GUI)};
\node[layer, fill=teal!15]   (pipeline) at (0, -1.8)  {Strat de Orchestrare};
\node[layer, fill=orange!15] (core)     at (0, -3.6)  {Strat de Procesare};
\node[layer, fill=gray!15]   (infra)    at (0, -5.4)  {Strat de Infrastructură};

\draw[->, thick] (gui.south)      -- (pipeline.north);
\draw[->, thick] (pipeline.south) -- (core.north);
\draw[->, thick] (core.south)     -- (infra.north);

\end{tikzpicture}
\caption{Arhitectura pe straturi a aplicației. Dependențele sunt direcționate exclusiv de sus în jos.}
\label{fig:layers}
\end{figure}

\section{Diagrama de Secvență}

Figura~\ref{fig:seqdiagram} ilustrează interacțiunea dintre componente pentru scenariul central al aplicației: pornirea sistemului de către utilizator și procesarea continuă a cadrelor video până la oprire.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[>=Stealth, font=\scriptsize]

% ── Participant boxes ──────────────────────────────────────────────────
\node[draw, fill=gray!20, minimum width=2.8cm, minimum height=0.65cm]
    (GUI)      at (1.8, 0)  {\textbf{SystemViewWidget}};
\node[draw, fill=gray!20, minimum width=2.8cm, minimum height=0.65cm]
    (PL)       at (6.5, 0)  {\textbf{DepthPipeline}};
\node[draw, fill=gray!20, minimum width=2.8cm, minimum height=0.65cm]
    (PROC)     at (11.2, 0) {\textbf{ModelRegistry + Core}};

% ── Lifelines ─────────────────────────────────────────────────────────
\draw[dashed, gray!60] (1.8,  -0.33) -- (1.8,  -13.5);
\draw[dashed, gray!60] (6.5,  -0.33) -- (6.5,  -13.5);
\draw[dashed, gray!60] (11.2, -0.33) -- (11.2, -13.5);

% ── Activation boxes ──────────────────────────────────────────────────
\fill[white] (6.3, -0.9) rectangle (6.7, -12.2);
\draw        (6.3, -0.9) rectangle (6.7, -12.2);
\fill[white] (11.0, -2.7) rectangle (11.4, -3.4);
\draw        (11.0, -2.7) rectangle (11.4, -3.4);
\fill[white] (11.0, -5.0) rectangle (11.4, -9.5);
\draw        (11.0, -5.0) rectangle (11.4, -9.5);

% ── Initialization ────────────────────────────────────────────────────
\draw[->] (1.8,  -0.9) -- node[above]{\texttt{start()}} (6.3, -0.9);
\draw[->] (6.7,  -1.7) -- node[above]{\texttt{load\_calibration()}} (11.0, -1.7);
\draw[->, dashed] (11.0, -2.1) -- node[above]{calibration data} (6.7, -2.1);
\draw[->] (6.7,  -2.7) -- node[above]{\texttt{load\_model()}} (11.0, -2.7);
\draw[->, dashed] (11.0, -3.4) -- node[above]{model ready} (6.7, -3.4);

% ── Loop box ──────────────────────────────────────────────────────────
\draw[thick] (0.2, -4.2) rectangle (12.8, -11.3);
\node[anchor=north west, font=\scriptsize\itshape] at (0.2, -4.2) {loop [fiecare cadru]};

% ── Per-frame messages ────────────────────────────────────────────────
\draw[->] (6.7,  -5.0) -- node[above]{\texttt{estimate\_depth(frame)}} (11.0, -5.0);
\draw[->, dashed] (11.0, -6.0) -- node[above]{\texttt{depth\_map}} (6.7, -6.0);
\draw[->] (6.7,  -7.0) -- node[above]{\texttt{detect\_ground\_mask(depth\_map)}} (11.0, -7.0);
\draw[->, dashed] (11.0, -8.0) -- node[above]{\texttt{ground\_mask}} (6.7, -8.0);
\draw[->] (6.7,  -9.0) -- node[above]{\texttt{find\_path(ground\_mask)}} (11.0, -9.0);
\draw[->, dashed] (11.0, -9.5) -- node[above]{\texttt{FrameResult}} (6.7, -9.5);

% ── Qt signal to GUI ──────────────────────────────────────────────────
\draw[->, decorate, decoration={snake, amplitude=1pt, segment length=5pt}]
    (6.3, -10.8) -- node[above]{\texttt{result\_signal(FrameResult)}} (1.8, -10.8);

% ── Stop ──────────────────────────────────────────────────────────────
\draw[->] (1.8, -12.2) -- node[above]{\texttt{stop()}} (6.3, -12.2);

% ── Lifeline terminators ──────────────────────────────────────────────
\draw[thick] (1.6,-13.5) -- (2.0,-13.5);
\draw[thick] (6.3,-13.5) -- (6.7,-13.5);
\draw[thick] (11.0,-13.5) -- (11.4,-13.5);

\end{tikzpicture}
\caption{Diagrama de secvență pentru inițializare și procesarea continuă a cadrelor video. Săgeata ondulată reprezintă semnalul Qt asincron emis către GUI.}
\label{fig:seqdiagram}
\end{figure}

\section{Gestiunea Fișierelor de Calibrare (CRUD)}
\label{sec:crud}

Aplicația include un subsistem dedicat stocării și gestionării calibrărilor produse în urma sesiunilor de calibrare. Arhitectura acestuia urmează modelul clasic pe trei niveluri: domeniu, repository și serviciu.

\textbf{Modelul de date.} Entitatea centrală este \texttt{CalibrationRecord}, un dataclass pur Python fără dependențe ORM sau NumPy, care conține metadatele unei calibrări: scorul RMS, dimensiunile grilei (\texttt{cols}, \texttt{rows}), dimensiunea pătratului în milimetri, un nume și note opționale, data creării și calea către fișierul \texttt{.npz} asociat. Persistența se realizează printr-o bază de date SQLite (\texttt{calibrations.db}), gestionată prin SQLAlchemy, iar fișierele \texttt{.npz} sunt stocate fizic în directorul \texttt{calibration\_files/} sub un nume UUID unic generat la salvare.

\textbf{Nivelul de acces la date.} Clasa \texttt{CalibrationRepository} implementează operațiile de bază asupra bazei de date: \texttt{save()} pentru inserarea unui record nou, \texttt{find\_by\_id()} și \texttt{find\_all()} pentru interogare, \texttt{update()} pentru modificarea metadatelor și \texttt{delete()} pentru ștergerea unui rând. Conversia între obiectul de domeniu și modelul ORM este realizată prin metodele statice \texttt{\_to\_model()} și \texttt{\_to\_domain()}, menținând astfel o separare strictă între straturile de persistență și logica aplicației.

\textbf{Nivelul de serviciu.} \texttt{CalibrationLibraryService} expune operațiile CRUD complete cu validare integrată:
\begin{itemize}
    \item \textbf{Create} — \texttt{add\_from\_calibration()} copiază automat fișierul \texttt{.npz} produs de o sesiune de calibrare în directorul gestionat și salvează metadatele în baza de date; \texttt{import\_file()} permite importul unui fișier extern ales de utilizator, extragând RMS-ul și parametrii grilei direct din fișier dacă sunt disponibili;
    \item \textbf{Read} — \texttt{list\_all()} returnează toate calibrările ordonate descrescător după dată; \texttt{apply()} copiază fișierul \texttt{.npz} al unei calibrări selectate în locația așteptată de pipeline (\texttt{calibration.npz}), activând-o; \texttt{export()} permite salvarea fișierului într-o destinație aleasă de utilizator;
    \item \textbf{Update} — \texttt{update\_metadata()} permite editarea numelui și a notelor asociate unei înregistrări, cu validare de lungime și conținut;
    \item \textbf{Delete} — \texttt{delete()} șterge atât înregistrarea din baza de date, cât și fișierul \texttt{.npz} de pe disc, asigurând consistența între cele două spații de stocare.
\end{itemize}

Validarea este delegată clasei \texttt{CalibrationValidator}, care verifică RMS-ul, integritatea fișierului \texttt{.npz} și lungimea câmpurilor text înainte de orice operație de scriere.

\section{Testarea Automată a Componentelor}
\label{sec:testing}

Separarea strictă a responsabilităților nu este doar un principiu de proiectare, ci și o condiție care face codul testabil în izolare. Deoarece stratul de persistență nu depinde de interfața grafică, de cameră sau de modelul de adâncime, operațiile sale pot fi verificate automat fără a porni întreaga aplicație. Subsistemul de gestiune a calibrărilor (Secțiunea~\ref{sec:crud}), fiind componenta cu cea mai bogată logică de tip CRUD și cu efecte atât asupra bazei de date, cât și asupra sistemului de fișiere, a fost ales drept țintă principală a testării.

\textbf{Framework-ul de testare.} Testele sunt scrise folosind \texttt{pytest}, standardul de facto pentru testarea automată în Python. Spre deosebire de modulul \texttt{unittest} din biblioteca standard, \texttt{pytest} permite scrierea testelor ca funcții simple cu aserțiuni native (\texttt{assert}) și oferă mecanismul de \textit{fixtures}: blocuri de pregătire reutilizabile, injectate automat în testele care le solicită ca parametri.

\textbf{Izolarea prin fixtures.} Fiecare test rulează complet izolat, fără a atinge baza de date reală (\texttt{calibrations.db}) sau directorul de fișiere de producție. Fixture-ul \texttt{tmp\_path} oferit de \texttt{pytest} furnizează un director temporar unic per test, șters automat la final. Pe baza lui se construiesc: un motor SQLAlchemy legat de o bază de date SQLite temporară, cu tabelele create la cerere; un director temporar pentru fișierele \texttt{.npz}; o instanță de \texttt{CalibrationLibraryService} configurată să folosească aceste resurse efemere; și un fișier \texttt{.npz} valid de test, generat programatic cu NumPy. Astfel, testele sunt deterministe, independente între ele și repetabile.

\textbf{Acoperirea operațiilor CRUD.} Suita verifică operațiile pe ambele niveluri ale subsistemului:
\begin{itemize}
    \item la nivelul \texttt{CalibrationRepository} --- inserarea cu generarea automată a identificatorului și a datei, interogarea după identificator și listarea completă, actualizarea care modifică exclusiv numele și notele (păstrând neschimbate câmpurile tehnice) și ștergerea, inclusiv comportamentul corect pentru identificatori inexistenți;
    \item la nivelul \texttt{CalibrationLibraryService} --- crearea prin \texttt{import\_file()} și \texttt{add\_from\_calibration()} cu verificarea copierii fizice a fișierului în directorul gestionat, listarea, actualizarea metadatelor și ștergerea care elimină simultan înregistrarea din baza de date și fișierul \texttt{.npz} de pe disc;
    \item validările --- respingerea unui fișier inexistent, a unui scor RMS nerezonabil de mare și a unui nume care depășește limita de lungime, confirmând că erorile sunt semnalate prin excepția dedicată \texttt{CalibrationValidationError}.
\end{itemize}

Suita rezultată conține 18 teste, organizate conform Tabelului~\ref{tab:tests}, și se execută în mai puțin de o secundă, ceea ce o face potrivită pentru rulare frecventă în timpul dezvoltării.

\begin{table}[htbp]
\centering
\renewcommand{\arraystretch}{1.35}
\begin{tabular}{|l|c|p{6.5cm}|}
\hline
\textbf{Modul} & \textbf{Teste} & \textbf{Ce verifică} \\
\hline
\texttt{conftest.py} & --- & Fixtures comune: bază de date și director temporare, serviciul și fișierul \texttt{.npz} de test \\
\hline
\texttt{test\_calibration\_repository.py} & 8 & Operațiile CRUD direct pe baza de date, prin \texttt{CalibrationRepository} \\
\hline
\texttt{test\_calibration\_service.py} & 10 & CRUD prin \texttt{CalibrationLibraryService}, copierea/ștergerea fișierelor și validările \\
\hline
\end{tabular}
\caption{Structura suitei de teste pentru subsistemul de calibrări.}
\label{tab:tests}
\end{table}

\section{Instrumentarea pentru Profilarea Latenței}
\label{sec:profiling}

Pentru evaluarea cantitativă a performanței (prezentată în Capitolul~\ref{chap:ch5}), aplicația include un subsistem de instrumentare care măsoară durata fiecărei etape a pipeline-ului, cadru cu cadru. Proiectarea acestuia urmărește două cerințe aparent contradictorii: să ofere măsurători precise atunci când profilarea este activă, dar să nu introducă niciun cost atunci când este dezactivată, pentru a nu denatura comportamentul normal al sistemului.

\textbf{Strategie interschimbabilă.} Pipeline-ul comunică întotdeauna cu o interfață abstractă, \texttt{FrameProfiler}, fără a cunoaște implementarea concretă. Există două strategii (\textit{Strategy pattern}): \texttt{NullProfiler}, folosit când profilarea este dezactivată, ale cărui metode sunt operații vide (\textit{Null Object}) și care returnează un singur context reutilizabil, astfel încât calea critică nu plătește practic nimic; și \texttt{LatencyProfiler}, care cronometrează efectiv etapele. Funcția-fabrică \texttt{make\_profiler()} construiește strategia potrivită pe baza configurației, eliminând astfel ramificațiile condiționale din interiorul buclei de procesare.

\textbf{Măsurare per etapă.} Fiecare etapă sincronă a pipeline-ului, corecția distorsiunii, estimarea adâncimii, detecția solului și determinarea drumului, este încadrată într-un context manager \texttt{profiler.stage(name)}, care măsoară durata blocului cu \texttt{time.perf\_counter()}. Suma acestor patru etape constituie timpul total de procesare per cadru. Captura cadrului, care rulează pe firul de captură separat, este raportată informativ, dar exclusă din total. La finalul fiecărui cadru, \texttt{commit\_frame()} agregă timpii acumulați într-un singur eșantion.

\textbf{Scriere în loturi.} Pentru a evita operații de intrare/ieșire pe calea critică, eșantioanele sunt acumulate în memorie și scrise pe disc în loturi (implicit la fiecare 50 de cadre și la oprire). Fișierul CSV rezultat este denumit automat astfel încât să codifice profilul hardware și modul de adâncime sub care au fost colectate datele (de exemplu \texttt{latency\_nvidia\_metric\_20260604\_153012.csv}), păstrând măsurătorile legate de condițiile în care au fost obținute. Orice eroare de scriere este izolată, astfel încât profilarea să nu poată întrerupe niciodată pipeline-ul.

\textbf{Activare în timpul rulării.} Comutatorul \textit{Profile latency} din bara laterală a ecranului de vizualizare permite pornirea și oprirea profilării fără a reporni sistemul. Metoda \texttt{set\_profiling\_enabled()} înlocuiește referința strategiei active — pornirea inițiază un nou fișier CSV, iar oprirea îl golește și îl închide. Deoarece înlocuirea referinței este atomică sub GIL, operația este sigură față de firul de inferență care citește strategia activă.

\section{Fluxul de Date}

La pornirea sistemului, \texttt{MainWindow} încarcă configurația tipizată din \texttt{config.toml} printr-un apel la \texttt{AppConfig.load()}. Când utilizatorul apasă butonul \textit{START} din ecranul de vizualizare live, \texttt{SystemViewWidget} instanțiază un \texttt{DepthPipeline} și pornește firul de inferență. Acesta inițializează \texttt{ModelRegistry} (care, în funcție de profilul hardware, încarcă fie modelul PyTorch, fie fișierul HEF pentru Hailo), deschide camera prin \texttt{CameraFactory} și pornește firul de captură.

La fiecare cadru disponibil, pipeline-ul aplică corecția de distorsiune, rulează inferența pentru a obține harta de adâncime, detectează planul solului prin RANSAC vectorizat și construiește masca, planifică traiectoria prin A*, și împachetează toate rezultatele într-un obiect \texttt{FrameResult}. Dacă profilarea este activă, durata fiecăreia dintre aceste etape este măsurată și înregistrată (Secțiunea~\ref{sec:profiling}). Acesta este transmis stratului de prezentare prin semnalul Qt \texttt{result\_signal}, unde \texttt{SystemViewWidget} aplică suprapunerile vizuale și actualizează widget-ul de afișare.

Utilizatorul poate modifica parametrii sistemului în orice moment prin ecranul \textit{Settings}, care scrie valorile actualizate înapoi în \texttt{config.toml} și semnalizează ferestrei principale să reîncarce configurația. Modificările intră în vigoare la următoarea pornire a pipeline-ului.
